name: Azure CDN CI/CD

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
 #   - name: Set up Node.js
 #     uses: actions/setup-node@v3
 #     with:
 #       node-version: '16.x'
 #       cache: 'npm'
 #   - name: npm install, build
 #     run: |
 #       npm install
 #       npm run build --if-present
    - uses: azure/login@v1
      with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
    - name: Upload to blob storage
      uses: azure/CLI@v1
      with:
        inlineScript: az storage blob upload-batch --account-name bmeyerpublic --auth-mode key -d icons -s dist
  #  - name: Purge CDN endpoint
  #    uses: azure/CLI@v1
  #    with:
  #      inlineScript: |
  #         az cdn endpoint purge --content-paths  "/*" --profile-name "CDN_PROFILE_NAME" --name "CDN_ENDPOINT" --resource-group "RESOURCE_GROUP"
    - name: logout
      run: az logout
      if: always()
